import 'package:flutter/material.dart';

class CappccuinoCard extends StatelessWidget {
  const CappccuinoCard({super.key});

  @override
  Widget build(BuildContext context) {
    return GestureDetector(
      onTap:() {
        
      },
      child: AnimatedContainer(
        duration: Duration(
          milliseconds: 300))
    );
  }
}