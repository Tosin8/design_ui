import 'package:flutter/material.dart';

// using simple animation pub.dev
class FadeAnimation extends StatelessWidget {
  final double delay; 
  final Widget child; 
  const FadeAnimation({super.key, this.delay, this.child});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}