import 'package:flutter/material.dart';

class FurnitureOnboarding extends StatelessWidget {
  const FurnitureOnboarding({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}